package com.leetcode.recursion;

// 递归模板
public class Recursion {
    // 递归思维要点
    // 1. 不用进行人肉递归（最大误区）
    // 2. 找到最近最简方法，将其拆解成可重复解决的问题（重复子问题）
    // 3. 数学归纳法思维
    public static void main(String[] args) {

    }

    public static void recursion(int level) {
        //1. recursion terminate  递归的终结条件
//        if (level <= 0) {
//            return;
//        }

        //2. process logic in current level 处理当前逻辑
//        process(level, data)

        //3. drill down 下探到下一层
//        recursion(level--, data);

        //4. recursion the current level status if needed 清理当前层
    }
}
